import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'angularJs_CaridApp_Integradora',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
